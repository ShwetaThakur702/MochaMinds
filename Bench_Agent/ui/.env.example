# Copy this file to .env and adjust if your backend runs on a different port or host.
# The default (127.0.0.1:8000) works out of the box — only change if needed.
VITE_API_TARGET=http://127.0.0.1:8000
